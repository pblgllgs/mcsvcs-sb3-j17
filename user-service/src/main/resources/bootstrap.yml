server:
  port: 8083
spring:
  application:
    name: users-ws
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: local
clients:
  albums:
    url: http://albums-ws